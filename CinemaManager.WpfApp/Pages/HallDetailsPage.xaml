<Page x:Class="CinemaManager.WpfApp.Pages.HallDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Hall Details">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Hall name -->
        <TextBlock x:Name="HallNameText"
                   FontSize="28"
                   FontWeight="Bold"
                   Grid.Row="0"/>

        <!-- Hall info -->
        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock x:Name="HallTypeText" FontSize="14"/>
            <TextBlock x:Name="HallSeatsText" FontSize="14"/>
            <TextBlock x:Name="TotalDurationText" FontSize="14"/>
        </StackPanel>

        <!-- Sessions header -->
        <TextBlock Text="Sessions:"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,10"
                   Grid.Row="2"/>

        <!-- Sessions list -->
        <ListBox x:Name="SessionsListBox"
                 Grid.Row="3"
                 SelectionChanged="SessionsListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="{Binding MovieName}" FontSize="16" FontWeight="Bold"/>
                        <TextBlock>
                            <Run Text="Genre: "/><Run Text="{Binding Genre}"/>
                            <Run Text=" | Year: "/><Run Text="{Binding ReleaseYear}"/>
                        </TextBlock>
                        <TextBlock>
                            <Run Text="Start: "/><Run Text="{Binding StartTime, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
